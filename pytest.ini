[pytest]
testpaths = tests
addopts = -ra --tb=short
filterwarnings =
    ignore::DeprecationWarning
# Test env vars are injected by tests/conftest.py (top-of-module sys
# manipulation runs before any `app.*` import).
